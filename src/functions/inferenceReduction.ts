import { Puzzle, Section, Square } from "../types";
import inferenceDS from "./inferenceDS";

const assumer = (unAssumedPuzzle: Section) => {};

const inferenceReductio = (puzzle: Puzzle) => {
  //   const assumption = assumer();
  //   const syllogizedPuzzle = inferenceDS();
  //   const reductio = reductioCheck();
  //   const completed = completeCheck();
  //   if (!reductio && !completed) {
  //     inferenceReductio;
  //   }
};

export default inferenceReductio;
